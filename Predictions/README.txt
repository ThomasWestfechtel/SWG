To Extract Zero-Shot Predictions run:
The number at the end stands for the domain within the dataset.

For OfficeHome:
bash ext_oh.sh 0
For VisDA:
bash ext_vis.sh 0
For DomainNet:
bash ext_dn.sh 0

The DAPL predictions were generated with the offical implementation:
https://github.com/LeapLabTHU/DAPrompt
